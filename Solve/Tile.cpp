// tiles are a side of a cube. we do not need the other 
// sides anymore now that we have already rolled

// tiles keep track of their 4 neighbors as they are added
// and then determine if they are still in a valid word